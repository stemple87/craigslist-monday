{{#if addNewCategory}}
  <h1>New Category</h1>
    <div>
      <form>
        <div class="form-group">
          <label for="name">Name</label>
          {{input value=name id="name"}}
        </div>

        <button {{action 'newCategoryMethod'}}>Save</button>
      </form>
    </div>
{{else}}
    <button {{action 'categoryFormShow'}}>New Category</button>
{{/if}}
