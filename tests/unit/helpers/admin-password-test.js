import { adminPassword } from 'super-posts/helpers/admin-password';
import { module, test } from 'qunit';

module('Unit | Helper | admin password');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = adminPassword([42]);
  assert.ok(result);
});
